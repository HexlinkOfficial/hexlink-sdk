{
  "description": "Hexlink Account SDK",
  "main": "./lib/index.js",
  "module": "./lib.esm/index.js",
  "types": "./lib/index.d.ts",
  "name": "@hexlink/sdk",
  "devDependencies": {
    "@types/node": "^20.4.5",
    "gts": "^5.0.0",
    "typescript": "~5.1.6"
  },
  "scripts": {
    "build": "tsc --build ./tsconfig.json",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "sideEffects": false,
  "version": "0.0.1",
  "files": [
    "/lib.esm"
  ],
  "engines": {
    "node": ">=10.6.0"
  },
  "dependencies": {
    "@account-abstraction/contracts": "^0.6.0",
    "@dauth/core": "^0.2.2",
    "@hexlink/contracts": "^0.9.7",
    "axios": "^1.4.0",
    "ethers": "^6.7.1"
  }
}
